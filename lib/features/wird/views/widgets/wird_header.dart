import 'package:wadhakir/core/widgets/branded_header.dart';

/// The wird screens use the shared [BrandedHeader]. Kept as an alias so the
/// existing call sites (`WirdHeader(...)`) continue to work.
typedef WirdHeader = BrandedHeader;
