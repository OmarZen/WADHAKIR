name: wadhakir
description: "Complete Islamic companion: Prayer times, Quran, Azkar, Radio & Qibla finder"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 2.3.5+12

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.24.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # State management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  
  # Local storage
  shared_preferences: ^2.5.4
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.0
  uuid: ^4.5.1
  
  # Sharing functionality
  share_plus: ^12.0.1
  
  # Qibla compass
  flutter_compass: ^0.8.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  vector_math: ^2.1.4
  adhan_dart: ^1.2.0
  syncfusion_flutter_datepicker: ^32.1.25
  syncfusion_flutter_core: ^32.1.25
  syncfusion_localizations: ^32.1.25
  intl: ^0.20.2
  
  # Quran Library (pub.dev package)
  quran_library: ^3.1.0
  
  # Notifications
  awesome_notifications: ^0.10.1
  windows_notification: ^1.3.0
  timezone: ^0.11.0
  permission_handler: ^11.3.1
  rxdart: ^0.28.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  
  # UI Components
  flutter_launcher_icons: ^0.14.4
  just_audio: ^0.10.5
  audio_session: ^0.1.25
  flutter_volume_controller: ^1.3.3
  sensors_plus: ^7.0.0
  flutter_native_splash: ^2.4.7
  url_launcher: ^6.3.2
  http: ^1.6.0
  cached_network_image: ^3.4.1
  bloc: ^9.0.0
  connectivity_plus: ^7.0.0
  flutter_dotenv: ^6.0.0
  google_nav_bar: ^5.0.7
  font_awesome_flutter: ^10.12.0
  country_flags: ^4.1.1
  home_widget: ^0.9.0
  path_provider: ^2.1.5
  hugeicons: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

  # JSON serialization generators
  json_serializable: ^6.7.1
  
  # MSIX packaging for Windows
  msix: ^3.16.12

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/mosques/
    - assets/images/profile/
    - assets/lang/
    - assets/json_data/
    - assets/json_data/history.json
    - assets/json_data/hadiths_json/
    - assets/json_data/hadiths_json/bukhari_books/arabic/
    - assets/json_data/hadiths_json/bukhari_books/english/
    - assets/json_data/hadiths_json/bukhari_books/urdu/
    - assets/json_data/hadiths_json/bukhari_books/bangla/
    - assets/json_data/hadiths_json/muslim_books/arabic/
    - assets/json_data/hadiths_json/muslim_books/english/
    - assets/json_data/hadiths_json/abudawud_books/arabic/
    - assets/json_data/hadiths_json/abudawud_books/english/
    - assets/json_data/hadiths_json/abudawud_books/urdu/
    - assets/json_data/hadiths_json/tirmidhi_books/arabic/
    - assets/json_data/hadiths_json/tirmidhi_books/english/
    - assets/json_data/hadiths_json/ibnmajah_books/arabic/
    - assets/json_data/hadiths_json/ibnmajah_books/english/
    - assets/json_data/hadiths_json/ibnmajah_books/urdu/
    - assets/json_data/hadiths_json/nasai_books/arabic/
    - assets/json_data/hadiths_json/nasai_books/english/
    - assets/json_data/hadiths_json/malik_books/arabic/
    - assets/json_data/hadiths_json/malik_books/english/
    - assets/json_data/hadiths_json/riyadussalihin_books/arabic/
    - assets/json_data/hadiths_json/riyadussalihin_books/english/
    - assets/json_data/hadiths_json/forty_books/arabic/
    - assets/json_data/hadiths_json/forty_books/english/
    - assets/json_data/hadiths_json/bulugh_books/arabic/
    - assets/json_data/hadiths_json/bulugh_books/english/
    - assets/json_data/hadiths_json/adab_books/arabic/
    - assets/json_data/hadiths_json/adab_books/english/
    - assets/json_data/hadiths_json/shamail_books/arabic/
    - assets/json_data/hadiths_json/shamail_books/english/
    - assets/json_data/hadiths_json/mishkat_books/arabic/
    - assets/json_data/hadiths_json/mishkat_books/english/
    - assets/json_data/hadiths_json/ahmad_books/arabic/
    - assets/json_data/hadiths_json/ahmad_books/english/
    - assets/json_data/hadiths_json/darimi_books/arabic/
    - assets/adhan_sounds/
    - assets/icons/icon.png
    - assets/icons/custom_menu.svg
    - assets/icons/custom_audio.svg
    - assets/logo.png
    - lib/features/radio/api_response.json
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Almarai
      fonts:
        - asset: assets/fonts/Almarai/Almarai-Regular.ttf
          weight: 400
        - asset: assets/fonts/Almarai/Almarai-Bold.ttf
          weight: 700
    - family: Jomhuria
      fonts:
        - asset: assets/fonts/Jomhuria/Jomhuria-Regular.ttf
          weight: 400
    - family: ScheherazadeNew
      fonts:
        - asset: assets/fonts/ScheherazadeNew/ScheherazadeNew-Regular.ttf
          weight: 400
        - asset: assets/fonts/ScheherazadeNew/ScheherazadeNew-Bold.ttf
          weight: 700

msix_config:
  display_name: Wadhakir
  publisher_display_name: Omar Waleed
  publisher: CN=DF776E6F-2B42-431A-B1DB-7378360B1BC1
  identity_name: OmarWaleed.Wadhakir
  msix_version: 2.3.5.0
  capabilities: "internetClient,location"
  
  # Store submission settings
  store: true
  architecture: x64
  
  # Languages
  languages: ar, en-us
  
  # App description
  description: >
    WADHAKIR is a complete Islamic companion app featuring prayer times, 
    Quran, Azkar, Islamic radio, and Qibla finder.
  
  # Logo configuration
  logo_path: assets/icons/icon.png
  icons_background_color: '#FFFFFF'
  
  # Certificate settings (not needed for Store, will be signed by MS)
  install_certificate: false